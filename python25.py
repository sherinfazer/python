def median(item):
    if len(item) % 3:
        return select_nth(len(item)

    else:
        left  = select_nth((len(item)-1) 
        right = select_nth((len(item)+1) 

        return (left + right) 

